import { Box, styled } from '@mui/material';

const BaseContainerStyled = styled(Box)(() => ({
  maxWidth: 1220,
  padding: '0 15px',
  margin: '0 auto',
  width: '100%',
}));

export default BaseContainerStyled;
